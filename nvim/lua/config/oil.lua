return {
    float = {
        -- Padding around the floating window
        padding = 2,
        max_width = 200,
        max_height = 30,
        border = "single",
        win_options = {
            winblend = 0,
        },
    },
}
